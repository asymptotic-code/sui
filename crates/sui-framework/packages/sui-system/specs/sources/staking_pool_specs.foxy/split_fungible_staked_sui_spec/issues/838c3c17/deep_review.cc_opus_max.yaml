true_issue: true
issue_in: spec
severity: high
confidence: high
reasoning: 'Confirmed by reading specs/sources/staking_pool_specs.move directly: the
  spec body is exactly `asserts(split_amount <= fungible_staked_sui_value(...))` followed
  by the target call, with ZERO ensures. So the entire functional contract is unverified
  — the primary state change (fungible_staked_sui.value == old(value) - split_amount),
  the new object (result.value == split_amount), pool_id preservation (result.pool_id
  == old.pool_id), and the conservation invariant (old(value) == new(value) + result.value).
  The issue is real, not a false positive: only the single abort guard is modeled.
  This is case (a), a SPEC gap, not a code defect. The implementation in sources/staking_pool.move
  is demonstrably correct: the assert guarantees split_amount <= value so `value =
  value - split_amount` cannot underflow; the new FungibleStakedSui is built with
  value=split_amount, pool_id copied unchanged, and a fresh UID; conservation holds
  trivially since (value - split_amount) + split_amount == value. The required accessors
  already exist (`fungible_staked_sui_value` -> .value, `fungible_staked_sui_pool_id`
  -> .pool_id), so the four ensures are directly writable. Applying the test for (a)
  vs (b): if the spec is tightened with those ensures, the prover would PASS and reveal
  no system bug — everything about the implementation remains true. That rules out
  (b). Severity is high despite the code being sound: FungibleStakedSui.value is an
  economically meaningful fungible pool-token amount, and conservation-of-value across
  a split is precisely the property formal verification exists to guarantee for token-like
  objects. The gap is total (not partial), so the spec currently offers zero protection
  against a future value-creation/accounting regression (e.g. a missing decrement
  or result.value = split_amount + 1 would slip through). The `@VERIFY(🛡️/✅)` marker
  overstates completeness — abort modeling may be done, but the semantics/ensures
  stage is empty. Confidence high: the absence of ensures is directly observable and
  the writable accessors are confirmed present.'
