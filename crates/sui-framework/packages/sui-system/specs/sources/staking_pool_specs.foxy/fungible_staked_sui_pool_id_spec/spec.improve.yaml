starting_state: stub
final_state: asserts_only
changes_made:
- Removed ignore_abort from the spec attribute
- Added @VERIFY(🛡️/✅) annotation marking abort conditions fully modeled
- 'No asserts needed: target is a trivial field read (fungible_staked_sui.pool_id)
  with zero abort paths'
prover_runs: 6
prover_final: pass
issues:
- 'Could not add a meaningful ensures: the only accessor for the private FungibleStakedSui.pool_id
  field is the target function itself (via a `use fun` alias), so referencing it in
  ensures triggers ''Underlying func is calling few times''.'
- Adding a fresh public/test_only getter to the source module did not help -- the
  prover compiles the spec against a prebuilt sui-system framework artifact, so newly-added
  source functions are 'unbound'. Source change was reverted; source diff is empty.
- 'All three phases (_Check, _Assume, _SpecNoAbortCheck) each PASS, but the cloud
  backend flakes intermittently: each run exactly one (rotating) phase fails in 0.1s
  with ''out of resources/timeout'' -- an infra flake, not a proof failure. Across
  runs _Check passed 3/3, _Assume 2/3, _SpecNoAbortCheck verified passing on its own
  runs.'
duration_seconds: 600
